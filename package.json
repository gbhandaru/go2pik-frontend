{
  "name": "go2pik-frontend",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode production",
    "build:preview": "vite build --mode preview",
    "build:production": "vite build --mode production",
    "deploy:preview": "npm run build:preview && firebase deploy --only hosting:ui_preview",
    "deploy:production": "npm run build:production && firebase deploy --only hosting:live",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.3"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.4",
    "@vitejs/plugin-react": "^4.2.0",
    "tailwindcss": "^4.2.4",
    "vite": "^5.0.0"
  }
}
